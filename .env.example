# Database connection string (ODBC / SQL Server)
DB_CONNECTION_STRING=Driver={ODBC Driver 18 for SQL Server};Server=localhost;Database=bakery;Trusted_Connection=yes;Encrypt=yes;TrustServerCertificate=yes;MARS_Connection=yes;

# JWT secret key used for signing tokens
JWT_SECRET=your_jwt_secret_here